include <bits/stdc++.h>
using namespace std;

struct node
{
    int data;
    node* left=NULL;
    node* right=NULL;

    node(int x)
    {
        data=x;
    }
}
Node* 
int main()
{
    freopen("inpout")
    node * x = new node(7);
    cout<<x->data;

    return 0;
}